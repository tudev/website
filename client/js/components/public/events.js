/** @jsx React.DOM */
var React   = require('react');

var Events = React.createClass({
    render: function() {
        return (
            <div></div>
        );
    }
});

module.exports = Events;