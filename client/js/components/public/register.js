/** @jsx React.DOM */
var React   = require('react');

var Register = React.createClass({
    render: function() {
        return (
            <div></div>
        );
    }
});

module.exports = Register;