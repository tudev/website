/** @jsx React.DOM */
var React   = require('react');

var Contact = React.createClass({
    render: function() {
        return (
            <div></div>
        );
    }
});

module.exports = Contact;